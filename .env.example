# Immunosuppression Trough Agent - Environment Configuration
# Copy this file to .env and modify values for your environment

# HMAC-SHA256 Audit Trail Secret Key
# IMPORTANT: Generate a strong random key for production use:
#   python -c "import secrets; print(secrets.token_hex(32))"
AUDIT_SECRET_KEY=change-me-generate-a-strong-random-key

# LLM Provider (mock, ollama, claude, openai)
MODEL_PROVIDER=mock

# API Server Configuration
API_HOST=127.0.0.1
API_PORT=8000
